"""PortCast export server.

A small FastAPI app that authenticates a Spotify user via OAuth and
returns their library as a PortCast document. See README.md for the
deployment model.
"""

__version__ = "0.1.0"
